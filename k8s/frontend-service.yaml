apiVersion: v1
kind: Service
metadata:
  name: mindx-web-service
  labels:
    app: mindx-web
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
  selector:
    app: mindx-web