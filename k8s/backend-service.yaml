apiVersion: v1
kind: Service
metadata:
  name: mindx-api-service
  labels:
    app: mindx-api
spec:
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
  selector:
    app: mindx-api